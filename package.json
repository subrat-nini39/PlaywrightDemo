{
  "name": "playwright-pom-ts",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report",
    "install-all": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.1"
  }
}
